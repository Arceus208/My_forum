mutation UpdatePost($id: Int!, $title: String!, $text: String!) {
  updatePost(id: $id, text: $text, title: $title) {
    post {
      id
      title
      text
    }
    errors {
      field
      message
    }
  }
}
